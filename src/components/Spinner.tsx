import React from "react";
import CircularIndeterminate from "./CircularSpinner";

const Spinner = () => (
  <div className="spinner">
    <CircularIndeterminate />
  </div>
);

export default Spinner;
